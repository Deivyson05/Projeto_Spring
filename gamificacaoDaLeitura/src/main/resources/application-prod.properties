# 🔷 Identificação do app
spring.application.name=gamificacaoDaLeitura
server.port=${PORT:8080}
server.address=0.0.0.0

# 🔶 Configuração do banco (Supabase PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${DB_USERNAME:postgres.trtqlarsnsdinqkqgwpk}
spring.datasource.password=${DB_PASSWORD:YomuSupa#62}
spring.datasource.driver-class-name=org.postgresql.Driver

# 🔶 JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=true
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.properties.hibernate.format_sql=false

# 🔶 Pool de conexões (HikariCP) - Otimizado para produção
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.connection-timeout=30000

# ⚠️ IMPORTANTE: Desabilita prepared statements para Supabase/PgBouncer
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preferQueryMode=simple

# 🔶 Logs (desabilitados em produção)
spring.jpa.show-sql=false
logging.level.root=INFO
logging.level.br.com.yomu.gamificacaoDaLeitura=INFO
logging.level.org.hibernate.SQL=WARN

# 🔶 Swagger / Documentação
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.enabled=true